async function toggleFamilyRecycle(element) {
    // 打开时值为 1，关闭时值为 0
    element.value = element.checked ? "1" : "0";
    console.log("当前状态值为：" + element.value);
}

async function toggleRecycle(element) {
    // 打开时值为 1，关闭时值为 0
    element.value = element.checked ? "1" : "0";
    console.log("当前状态值为：" + element.value);
}